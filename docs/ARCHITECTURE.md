# Architecture

OASYS Invest is a Tauri desktop app that wraps the investment web app unchanged. The
Tauri window loads the engine's own front end, so the desktop app and the website are
the same UI.

